const config = {
    db: {
      host: 'localhost',
      user: 'root',
      password: '',
      database: 'brokeNoMore'
    },
    auth: {
      accessTokenSecret: 'ToChange123'
    }
}

module.exports = config;